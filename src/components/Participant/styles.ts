import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    backgroundColor: '#343A40',
    flexDirection: 'row',
    borderRadius: 4,
    alignItems: 'center',
    padding: 12
  },
  name: {
    flex: 1,
    color: '#fff',
    fontSize: 16
  }
})
